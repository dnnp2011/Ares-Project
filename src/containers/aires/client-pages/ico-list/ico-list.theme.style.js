const styles = theme => ({
  background: {
    background: theme.palette.primary.main,
    width: '100%',
    height: '100%',
    overflow: 'auto',
  },
  'primary-card': {
    background: theme.palette.primary.light,
  }
});

export default styles;
