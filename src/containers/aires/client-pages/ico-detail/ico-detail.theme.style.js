const styles = theme => ({
  background: {
    background: theme.palette.secondary.main,
    width: '100%',
    height: '100%',
    overflowY: 'auto',
    overflowX: 'hidden',
  },
  'primary-card': {
    background: theme.palette.primary.light,
  }
});

export default styles;
