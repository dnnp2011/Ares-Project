const styles = theme => ({
  background: {
    background: theme.palette.secondary.main,
    width: 'auto',
    height: 'auto',
    display: 'flex',
    overflow: 'auto'
  },
  'primary-card': {
    background: theme.palette.primary.light,

  }
});

export default styles;
