const styles = theme => ({
  background: {
    background: theme.palette.secondary.main,
    width: '100%',
    height: '180%',
    display: 'flex',
  },
  'primary-card': {
    background: theme.palette.primary.light,
  }
});

export default styles;
