const styles = theme => ({
  background: {
    background: theme.palette.primary.main,
    width: '100%',
    height: '100%',
    overflowY: 'auto',
    overflowX: 'hidden',
  },
});

export default styles;
